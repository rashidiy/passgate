# pytest.ini
[pytest]
DJANGO_SETTINGS_MODULE = root.settings
python_files = tests.py test_*.py *_tests.py
markers =
    envfile(path): Use a specific .env file for this test module
    device(name): Device model name